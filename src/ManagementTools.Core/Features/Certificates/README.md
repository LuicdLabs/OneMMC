# Certificates

Certificates is currently a placeholder feature module.

Keep this folder minimal until real certificate-management functionality lands.
Do not add empty `Models`, `Services`, or `ViewModels` buckets preemptively.
