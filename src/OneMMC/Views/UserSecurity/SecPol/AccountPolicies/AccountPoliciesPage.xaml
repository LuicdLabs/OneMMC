<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="OneMMC.Views.AccountPoliciesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ctl="using:OneMMC.Controls"
    xmlns:converters="using:OneMMC.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:localization="using:OneMMC.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:secpol="using:OneMMC.Core.Features.UserSecurity.Models.SecPol"
    xmlns:vm="using:OneMMC.Core.Features.UserSecurity.ViewModels.SecPol"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Command Bar  -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton
                Click="EditButton_Click"
                Icon="Edit"
                IsEnabled="{x:Bind ViewModel.SelectedPolicy, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                Label="{x:Bind LocalizedStrings.Common_EditButton}" />
            <AppBarButton
                Click="RefreshButton_Click"
                Icon="Refresh"
                Label="{x:Bind LocalizedStrings.Common_Refresh}" />
            <AppBarButton Icon="More">
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Click="ExportButton_Click" Text="{x:Bind LocalizedStrings.SecPol_ExportButton}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xEDE1;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>

        <!--  Error InfoBar  -->
        <InfoBar
            x:Name="ErrorInfoBar"
            Title="{x:Bind LocalizedStrings.Common_ErrorTitle}"
            Grid.Row="1"
            Margin="24,0,24,12"
            IsClosable="True"
            IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
            Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
            Severity="Error" />

        <!--  Main Content  -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="300" />
            </Grid.ColumnDefinitions>

            <!--  Category Tree  -->
            <TreeView
                x:Name="PolicyTree"
                AllowDrop="False"
                CanDragItems="False"
                CanReorderItems="False"
                ItemInvoked="PolicyTree_ItemInvoked"
                ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                SelectionMode="Single">
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="vm:PolicyCategoryItem">
                        <Grid Height="36">
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind DisplayName}" />
                        </Grid>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <ctl:AotGridSplitter
                Grid.Column="1"
                Width="12"
                ResizeBehavior="BasedOnAlignment"
                ResizeDirection="Columns" />

            <!--  Policies List  -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                    x:Name="SearchBox"
                    Grid.Row="0"
                    Margin="12,8,12,4"
                    PlaceholderText="{x:Bind LocalizedStrings.Common_SearchPlaceholder}"
                    QueryIcon="Find"
                    TextChanged="SearchBox_TextChanged" />

                <ListView
                    x:Name="PoliciesList"
                    Grid.Row="1"
                    Padding="12"
                    DoubleTapped="PoliciesList_DoubleTapped"
                    ItemsSource="{x:Bind ViewModel.CurrentPolicies, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedPolicy, Mode=TwoWay}"
                    SelectionMode="Single">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="secpol:SecurityPolicyValue">
                            <StackPanel Margin="0,8" Spacing="4">
                                <TextBlock Text="{x:Bind Definition.DisplayName}" TextWrapping="Wrap" />
                                <TextBlock
                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind DisplaySetting}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!--  Loading Indicator  -->
                <ProgressRing
                    Grid.Row="1"
                    Width="50"
                    Height="50"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Grid>
</Page>
