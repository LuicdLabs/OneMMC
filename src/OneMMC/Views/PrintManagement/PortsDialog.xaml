<ContentDialog
    x:Class="OneMMC.Views.PortsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:OneMMC.Converters"
    xmlns:models="using:OneMMC.Core.Features.PrintManagement.Models"
    mc:Ignorable="d"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    CloseButtonText="{x:Bind LocalizedStrings.Common_CloseButton}">

    <ContentDialog.Resources>
        <converters:CollectionEmptyToVisibilityConverter x:Key="CollectionEmptyToVisibilityConverter"/>
        <converters:CollectionNotEmptyToVisibilityConverter x:Key="CollectionNotEmptyToVisibilityConverter"/>
        <converters:PrintPortPrinterConverter x:Key="PrintPortPrinterConverter"/>
    </ContentDialog.Resources>

    <StackPanel Spacing="12">
        <StackPanel Spacing="4">
            <TextBlock 
                Text="{x:Bind LocalizedStrings.PrintMgmt_PortsDialogTitle}"
                FontSize="18" 
                FontWeight="SemiBold"/>
            <TextBlock 
                Text="{x:Bind SubtitleText}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                FontSize="12"/>
        </StackPanel>

        <Grid>
            <!-- Empty state -->
            <TextBlock 
                Text="{x:Bind LocalizedStrings.PrintMgmt_NoPortsFound}"
                FontStyle="Italic"
                Margin="0,8"
                Visibility="{x:Bind ViewModel.Ports.Count, Mode=OneWay, Converter={StaticResource CollectionEmptyToVisibilityConverter}}"/>

            <!-- Ports list -->
            <ListView 
                ItemsSource="{x:Bind ViewModel.Ports, Mode=OneWay}"
                SelectionMode="None"
                MaxHeight="500"
                Visibility="{x:Bind ViewModel.Ports.Count, Mode=OneWay, Converter={StaticResource CollectionNotEmptyToVisibilityConverter}}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="12,8"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:PrintPortInfo">
                        <StackPanel Spacing="2">
                            <TextBlock 
                                Text="{x:Bind PortName}"
                                FontWeight="SemiBold"
                                FontSize="14"/>
                            <TextBlock 
                                Text="{x:Bind DescriptionAndType}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                FontSize="12"/>
                            <TextBlock 
                                Text="{x:Bind PrinterNames, Converter={StaticResource PrintPortPrinterConverter}}"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </StackPanel>
</ContentDialog>
