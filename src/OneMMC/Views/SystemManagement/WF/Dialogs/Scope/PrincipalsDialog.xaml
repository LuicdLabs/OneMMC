<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="OneMMC.Views.Dialogs.Scope.PrincipalsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMMC.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    PrimaryButtonText="{x:Bind local:LocalizedStrings.Instance.Common_OKButton}"
    SecondaryButtonText="{x:Bind local:LocalizedStrings.Instance.Common_CancelButton}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
    </ContentDialog.Resources>

    <ScrollViewer
        MaxHeight="560"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled">
    <Grid MinWidth="480" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar
            x:Name="SecureConnectionInfoBar"
            Grid.Row="0"
            Margin="0,0,0,12"
            IsClosable="False"
            IsOpen="False"
            Message="You can specify user and computer authentication only when the action is set to &quot;Allow only secure connections.&quot;"
            Severity="Warning" />

        <TextBlock
            x:Name="DescriptionText"
            Grid.Row="1"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap" />

        <!-- Authorized principals -->
        <StackPanel Grid.Row="2" Margin="0,12,0,6" Spacing="4">
            <TextBlock x:Name="AuthorizedLabel" FontWeight="SemiBold" />
            <CheckBox x:Name="OnlyAllowCheckBox" Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_OnlyAllowConnectionsFromTheseUsers}"
                      Checked="OnlyAllowCheckBox_Changed" Unchecked="OnlyAllowCheckBox_Changed" />
        </StackPanel>

        <Border
            x:Name="AuthorizedSection"
            Grid.Row="3"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListView
                    x:Name="AuthorizedListView"
                    Grid.Row="0"
                    MaxHeight="140"
                    SelectionMode="Single" />
                <StackPanel
                    Grid.Row="1"
                    Padding="8"
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    Orientation="Horizontal"
                    Spacing="8">
                    <!--
                        Local Principals mode: DropDownButton with two options
                        Remote Users mode: plain Add button (AuthAddButton visible, AuthAddDropDown collapsed)
                    -->
                    <Button
                        x:Name="AuthAddButton"
                        Content="{x:Bind local:LocalizedStrings.Instance.Common_AddButton}"
                        Click="AuthAddButton_Click"
                        Visibility="Collapsed" />
                    <DropDownButton
                        x:Name="AuthAddDropDown"
                        Content="{x:Bind local:LocalizedStrings.Instance.Common_AddButton}">
                        <DropDownButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyoutItem
                                    x:Name="AuthAddLocalUserItem"
                                    Text="{x:Bind local:LocalizedStrings.Instance.WF_Principals_LocalUser}"
                                    Click="AuthAddLocalUserItem_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE77B;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem
                                    x:Name="AuthAddAppPackageItem"
                                    Text="{x:Bind local:LocalizedStrings.Instance.WF_Principals_ApplicationPackageProperties}"
                                    Click="AuthAddAppPackageItem_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE7B8;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                    <Button x:Name="AuthDeleteButton" Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" Click="AuthDeleteButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Exception principals -->
        <StackPanel Grid.Row="4" Margin="0,12,0,6" Spacing="4">
            <TextBlock x:Name="ExceptionLabel" FontWeight="SemiBold" />
            <CheckBox x:Name="SkipRuleCheckBox" Content="{x:Bind local:LocalizedStrings.Instance.WF_Principals_SkipThisRuleForConnectionsFromTheseUsers}"
                      Checked="SkipRuleCheckBox_Changed" Unchecked="SkipRuleCheckBox_Changed" />
        </StackPanel>

        <Border
            x:Name="ExceptionSection"
            Grid.Row="5"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListView
                    x:Name="ExceptionListView"
                    Grid.Row="0"
                    MaxHeight="120"
                    SelectionMode="Single" />
                <StackPanel
                    Grid.Row="1"
                    Padding="8"
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button
                        x:Name="ExcAddButton"
                        Content="{x:Bind local:LocalizedStrings.Instance.Common_AddButton}"
                        Click="ExcAddButton_Click"
                        Visibility="Collapsed" />
                    <DropDownButton
                        x:Name="ExcAddDropDown"
                        Content="{x:Bind local:LocalizedStrings.Instance.Common_AddButton}">
                        <DropDownButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyoutItem
                                    x:Name="ExcAddLocalUserItem"
                                    Text="{x:Bind local:LocalizedStrings.Instance.WF_Principals_LocalUser}"
                                    Click="ExcAddLocalUserItem_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE77B;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem
                                    x:Name="ExcAddAppPackageItem"
                                    Text="{x:Bind local:LocalizedStrings.Instance.WF_Principals_ApplicationPackageProperties}"
                                    Click="ExcAddAppPackageItem_Click">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE7B8;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                    <Button x:Name="ExcDeleteButton" Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" Click="ExcDeleteButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
    </ScrollViewer>
</ContentDialog>
