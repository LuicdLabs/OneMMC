<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="OneMMC.Views.Dialogs.Scope.CapabilitiesFlyoutContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMMC.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:LocalizedStrings x:Key="LocalizedStrings" />
    </UserControl.Resources>

    <StackPanel Width="320" Padding="12" Spacing="8">
        <TextBlock
            FontWeight="SemiBold"
            Text="{Binding WF_Capabilities_ChooseCapabilities, Source={StaticResource LocalizedStrings}}" />
        <TextBlock
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="{Binding WF_Capabilities_SelectTheApplicationPackageCapabilitiesToAuthorize, Source={StaticResource LocalizedStrings}}"
            TextWrapping="Wrap" />

        <Border
            Margin="0,4,0,0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4"
            MaxHeight="260">
            <ListView
                x:Name="CapabilitiesListView"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollMode="Enabled"
                SelectionMode="Multiple" />
        </Border>

        <StackPanel
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Name="CancelButton"
                Content="{Binding Common_CancelButton, Source={StaticResource LocalizedStrings}}"
                Click="CancelButton_Click" />
            <Button
                x:Name="OkButton"
                Content="{Binding Common_OKButton, Source={StaticResource LocalizedStrings}}"
                Style="{StaticResource AccentButtonStyle}"
                Click="OkButton_Click" />
        </StackPanel>
    </StackPanel>
</UserControl>
