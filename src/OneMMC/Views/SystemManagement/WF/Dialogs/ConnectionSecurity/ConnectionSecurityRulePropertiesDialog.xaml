<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="OneMMC.Views.Dialogs.ConnectionSecurity.ConnectionSecurityRulePropertiesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMMC.Localization"
    xmlns:csdialogs="using:OneMMC.Views.Dialogs.ConnectionSecurity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    PrimaryButtonText="{x:Bind local:LocalizedStrings.Instance.Common_OKButton}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
    </ContentDialog.Resources>

    <ScrollViewer
        MaxHeight="560"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled">
    <Grid MinWidth="480" MinHeight="420">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Rule name header  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,4"
            Orientation="Horizontal"
            Spacing="12">
            <FontIcon
                VerticalAlignment="Center"
                FontSize="28"
                Glyph="&#xE839;" />
            <TextBlock
                x:Name="RuleNameHeader"
                VerticalAlignment="Center"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_DefaultRuleName}"
                TextWrapping="Wrap" />
        </StackPanel>

        <!--  SelectorBar tabs  -->
        <SelectorBar
            x:Name="TabBar"
            Grid.Row="1"
            Margin="0,8,0,12"
            SelectionChanged="TabBar_SelectionChanged">
            <SelectorBarItem x:Name="GeneralTab" Text="{x:Bind local:LocalizedStrings.Instance.WF_Tab_General}" />
            <SelectorBarItem x:Name="AuthenticationTab" Text="{x:Bind local:LocalizedStrings.Instance.WF_Section_Authentication}" />
            <SelectorBarItem x:Name="AdvancedTab" Text="{x:Bind local:LocalizedStrings.Instance.WF_Tab_Advanced}" />
        </SelectorBar>

        <!--  Tab content  -->
        <Grid Grid.Row="2">

            <!--  ═══ General ═══  -->
            <ScrollViewer
                x:Name="GeneralPanel"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled">
                <StackPanel Padding="0,0,4,0" Spacing="4">

                    <!--  Endpoint / Protocol  -->
                    <Grid ColumnSpacing="16" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="170" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Endpoint1IPAddress}" />
                        <TextBlock
                            x:Name="Ep1IpValue"
                            Grid.Row="0"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Any}" />

                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Endpoint1Port}" />
                        <TextBlock
                            x:Name="Ep1PortValue"
                            Grid.Row="1"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Any}" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Endpoint2IPAddress}" />
                        <TextBlock
                            x:Name="Ep2IpValue"
                            Grid.Row="2"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Any}" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Endpoint2Port}" />
                        <TextBlock
                            x:Name="Ep2PortValue"
                            Grid.Row="3"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Any}" />

                        <TextBlock
                            Grid.Row="4"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Protocol}" />
                        <TextBlock
                            x:Name="ProtocolValue"
                            Grid.Row="4"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Any}" />
                    </Grid>

                    <Border
                        Height="1"
                        Margin="0,8,0,8"
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <!--  Profile  -->
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="170" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Profile}" />
                        <TextBlock
                            x:Name="ProfileValue"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_All}" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!--  ═══ Authentication ═══  -->
            <ScrollViewer
                x:Name="AuthenticationPanel"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled"
                Visibility="Collapsed">
                <StackPanel Padding="0,0,4,0" Spacing="16">

                    <!--  Requirements  -->
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_Requirements}" />
                        <TextBlock
                            x:Name="RequirementsValue"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_CSR_AuthMode_RequestInboundOutbound}"
                            TextWrapping="Wrap" />
                    </Grid>

                    <!--  First authentication  -->
                    <StackPanel Spacing="6">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_FirstAuthentication}" />
                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                            <ListView
                                x:Name="FirstAuthListView"
                                MinHeight="120"
                                MaxHeight="160"
                                SelectionMode="Single">
                                <ListView.Header>
                                    <Grid
                                        Padding="12,6"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                        ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Column="0"
                                            FontWeight="SemiBold"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Method}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            FontWeight="SemiBold"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Details}" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="csdialogs:AuthMethodItem">
                                        <Grid Padding="0,4" ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                Text="{x:Bind Name}"
                                                TextWrapping="Wrap" />
                                            <TextBlock
                                                Grid.Column="1"
                                                Text="{x:Bind Description}"
                                                TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>

                    <!--  Second authentication  -->
                    <StackPanel Spacing="6">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_SecondAuthentication}" />
                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                            <ListView
                                x:Name="SecondAuthListView"
                                MinHeight="120"
                                MaxHeight="160"
                                SelectionMode="Single">
                                <ListView.Header>
                                    <Grid
                                        Padding="12,6"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                        ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Column="0"
                                            FontWeight="SemiBold"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Method}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            FontWeight="SemiBold"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Details}" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="csdialogs:AuthMethodItem">
                                        <Grid Padding="0,4" ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                Text="{x:Bind Name}"
                                                TextWrapping="Wrap" />
                                            <TextBlock
                                                Grid.Column="1"
                                                Text="{x:Bind Description}"
                                                TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <!--  ═══ Advanced ═══  -->
            <ScrollViewer
                x:Name="AdvancedPanel"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled"
                Visibility="Collapsed">
                <StackPanel Padding="0,0,4,0" Spacing="8">
                    <Grid ColumnSpacing="16" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="200" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_LocalTunnelEndpoint}" />
                        <TextBlock
                            x:Name="LocalTunnelValue"
                            Grid.Row="0"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_None}" />

                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_RemoteTunnelEndpoint}" />
                        <TextBlock
                            x:Name="RemoteTunnelValue"
                            Grid.Row="1"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_None}" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_InterfaceTypes}" />
                        <TextBlock
                            x:Name="InterfaceTypesValue"
                            Grid.Row="2"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_AllInterfaceTypes}" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_ApplyAuthorization}" />
                        <TextBlock
                            x:Name="ApplyAuthValue"
                            Grid.Row="3"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_No}" />
                    </Grid>

                    <Border
                        Height="1"
                        Margin="0,4,0,4"
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="200" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontWeight="SemiBold"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_ConnectionSecurityRuleProperties_ExemptIPsecProtectedConnections}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            x:Name="ExemptIpsecValue"
                            Grid.Column="1"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_No}" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
    </ScrollViewer>
</ContentDialog>
