<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="OneMMC.Views.Dialogs.WFProperties.ProtectedNetworkConnectionsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMMC.Localization"
    xmlns:wfprof="using:OneMMC.Core.Features.SystemManagement.Models.WF.Profiles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    PrimaryButtonText="{x:Bind local:LocalizedStrings.Instance.Common_OKButton}"
    CloseButtonText="{x:Bind local:LocalizedStrings.Instance.Common_CancelButton}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
    </ContentDialog.Resources>

    <ScrollViewer
        MaxHeight="560"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled">
        <StackPanel MinWidth="500" Spacing="12">
            <TextBlock
                Text="{x:Bind local:LocalizedStrings.Instance.WF_ProtectedNetworkConnections_SelectTheCheckBoxForEachConnectionYouWantWindowsDefenderFirewall}"
                TextWrapping="Wrap" />

            <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_ProtectedNetworkConnections_NetworkConnections}" />

            <Border
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8">
                <ListView
                    x:Name="ConnectionsListView"
                    MinHeight="160"
                    MaxHeight="320"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="wfprof:NetworkConnectionItem">
                            <CheckBox
                                Content="{x:Bind Name}"
                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
