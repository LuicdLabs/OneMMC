<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="OneMMC.Views.Dialogs.WFProperties.IpsecTunnelAuthorizationsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneMMC.Localization"
    xmlns:wfprof="using:OneMMC.Core.Features.SystemManagement.Models.WF.Profiles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="TunnelAuthorizationItemTemplate" x:DataType="wfprof:TunnelAuthorizationItem">
            <TextBlock Text="{x:Bind Name}" />
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled">
        <Grid MinWidth="500" MinHeight="560">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="ValidationTextBlock"
                Grid.Row="0"
                Margin="0,0,0,10"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />

            <SelectorBar
                x:Name="TabBar"
                Grid.Row="1"
                Margin="0,0,0,10"
                SelectionChanged="TabBar_SelectionChanged">
                <SelectorBarItem x:Name="ComputersTab" Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Computers}" />
                <SelectorBarItem x:Name="UsersTab" Text="{x:Bind local:LocalizedStrings.Instance.WF_Common_Users}" />
            </SelectorBar>

            <Grid Grid.Row="2">
                <Grid x:Name="ComputersPanel">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                <Border
                    Grid.Row="0"
                    Margin="0,0,0,10"
                    Padding="10"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_AuthorizedComputers}" />
                        <CheckBox
                            x:Name="AllowComputersCheckBox"
                            Checked="AllowComputersCheckBox_Checked"
                            Content="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_OnlyAllowConnectionsFromTheseComputers}"
                            Unchecked="AllowComputersCheckBox_Checked" />

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                Grid.Column="0"
                                VerticalAlignment="Top"
                                FontSize="28"
                                Glyph="&#xE7F8;" />

                            <ListView
                                x:Name="AllowedComputersListView"
                                Grid.Column="1"
                                MinHeight="120"
                                ItemTemplate="{StaticResource TunnelAuthorizationItemTemplate}"
                                SelectionMode="Single" />

                            <StackPanel Grid.Column="2" Spacing="8">
                                <Button
                                    x:Name="AddAllowedComputerButton"
                                    MinWidth="96"
                                    Click="AddAllowedComputerButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_AddEllipsis}" />
                                <Button
                                    x:Name="RemoveAllowedComputerButton"
                                    MinWidth="96"
                                    Click="RemoveAllowedComputerButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border
                    Grid.Row="1"
                    Padding="10"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_Exceptions}" />
                        <CheckBox
                            x:Name="DenyComputersCheckBox"
                            Checked="DenyComputersCheckBox_Checked"
                            Content="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_DenyConnectionsFromTheseComputers}"
                            Unchecked="DenyComputersCheckBox_Checked" />

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                Grid.Column="0"
                                VerticalAlignment="Top"
                                FontSize="28"
                                Glyph="&#xE7F8;" />

                            <ListView
                                x:Name="DeniedComputersListView"
                                Grid.Column="1"
                                MinHeight="120"
                                ItemTemplate="{StaticResource TunnelAuthorizationItemTemplate}"
                                SelectionMode="Single" />

                            <StackPanel Grid.Column="2" Spacing="8">
                                <Button
                                    x:Name="AddDeniedComputerButton"
                                    MinWidth="96"
                                    Click="AddDeniedComputerButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_AddEllipsis}" />
                                <Button
                                    x:Name="RemoveDeniedComputerButton"
                                    MinWidth="96"
                                    Click="RemoveDeniedComputerButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                </Grid>

                <Grid x:Name="UsersPanel" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                <Border
                    Grid.Row="0"
                    Margin="0,0,0,10"
                    Padding="10"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_AuthorizedUsers}" />
                        <CheckBox
                            x:Name="AllowUsersCheckBox"
                            Checked="AllowUsersCheckBox_Checked"
                            Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_OnlyAllowConnectionsFromTheseUsers}"
                            Unchecked="AllowUsersCheckBox_Checked" />

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                Grid.Column="0"
                                VerticalAlignment="Top"
                                FontSize="28"
                                Glyph="&#xE77B;" />

                            <ListView
                                x:Name="AllowedUsersListView"
                                Grid.Column="1"
                                MinHeight="120"
                                ItemTemplate="{StaticResource TunnelAuthorizationItemTemplate}"
                                SelectionMode="Single" />

                            <StackPanel Grid.Column="2" Spacing="8">
                                <Button
                                    x:Name="AddAllowedUserButton"
                                    MinWidth="96"
                                    Click="AddAllowedUserButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_AddEllipsis}" />
                                <Button
                                    x:Name="RemoveAllowedUserButton"
                                    MinWidth="96"
                                    Click="RemoveAllowedUserButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border
                    Grid.Row="1"
                    Padding="10"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_Exceptions}" />
                        <CheckBox
                            x:Name="DenyUsersCheckBox"
                            Checked="DenyUsersCheckBox_Checked"
                            Content="{x:Bind local:LocalizedStrings.Instance.WF_IpsecTunnelAuthorizations_DenyConnectionsFromTheseUsers}"
                            Unchecked="DenyUsersCheckBox_Checked" />

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                Grid.Column="0"
                                VerticalAlignment="Top"
                                FontSize="28"
                                Glyph="&#xE77B;" />

                            <ListView
                                x:Name="DeniedUsersListView"
                                Grid.Column="1"
                                MinHeight="120"
                                ItemTemplate="{StaticResource TunnelAuthorizationItemTemplate}"
                                SelectionMode="Single" />

                            <StackPanel Grid.Column="2" Spacing="8">
                                <Button
                                    x:Name="AddDeniedUserButton"
                                    MinWidth="96"
                                    Click="AddDeniedUserButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.WF_Common_AddEllipsis}" />
                                <Button
                                    x:Name="RemoveDeniedUserButton"
                                    MinWidth="96"
                                    Click="RemoveDeniedUserButton_Click"
                                    Content="{x:Bind local:LocalizedStrings.Instance.Common_RemoveButton}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
    </ScrollViewer>
</UserControl>
