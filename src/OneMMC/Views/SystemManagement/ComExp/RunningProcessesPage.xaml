<Page
    x:Class="OneMMC.Views.ComExp.RunningProcessesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localization="using:OneMMC.Localization"
    xmlns:converters="using:OneMMC.Converters"
    xmlns:local="using:OneMMC.Core.Features.SystemManagement.Models.ComExp"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter" />

        <Style x:Key="PropertyHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="PropertyLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="Margin" Value="0,0,8,4" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>

        <Style x:Key="PropertyValueStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="IsTextSelectionEnabled" Value="True" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>
    </Page.Resources>

    <Grid RowDefinitions="Auto,*">
        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Icon="Refresh" Label="{x:Bind LocalizedStrings.ComExp_RefreshButton}" Click="RefreshButton_Click" />
        </CommandBar>

        <Grid Grid.Row="1" RowDefinitions="*,Auto" Padding="24,8,24,24">
            <Grid ColumnDefinitions="*" RowDefinitions="Auto,*">
                <Grid ColumnDefinitions="2*,2*,*,*,*,*">
                    <TextBlock Text="{x:Bind LocalizedStrings.ComExp_Process_Name}" FontWeight="SemiBold" Padding="8,4" />
                    <TextBlock Grid.Column="1" Text="{x:Bind LocalizedStrings.ComExp_Process_Executable}" FontWeight="SemiBold" Padding="8,4" />
                    <TextBlock Grid.Column="2" Text="{x:Bind LocalizedStrings.ComExp_Process_Id}" FontWeight="SemiBold" Padding="8,4" />
                    <TextBlock Grid.Column="3" Text="{x:Bind LocalizedStrings.ComExp_Process_IsPaused}" FontWeight="SemiBold" Padding="8,4" />
                    <TextBlock Grid.Column="4" Text="{x:Bind LocalizedStrings.ComExp_Process_IsRecycling}" FontWeight="SemiBold" Padding="8,4" />
                    <TextBlock Grid.Column="5" Text="{x:Bind LocalizedStrings.ComExp_Process_IsNTService}" FontWeight="SemiBold" Padding="8,4" />
                </Grid>

                <ListView
                    Grid.Row="1"
                    ItemsSource="{x:Bind ViewModel.Processes, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedProcess, Mode=TwoWay}"
                    SelectionMode="Single">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:ProcessInfo">
                            <Grid ColumnDefinitions="2*,2*,*,*,*,*" Padding="4,8">
                                <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" Text="{x:Bind ExecutableName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Text="{x:Bind ProcessId}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{x:Bind IsPaused, Converter={StaticResource BoolToYesNoConverter}}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="4" Text="{x:Bind IsRecycling, Converter={StaticResource BoolToYesNoConverter}}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="5" Text="{x:Bind IsNTService, Converter={StaticResource BoolToYesNoConverter}}" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Padding="8,12,8,0">
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
                <ProgressRing
                    Grid.Column="1"
                    Width="20"
                    Height="20"
                    IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Grid>
</Page>
