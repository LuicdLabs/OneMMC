<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="OneMMC.Views.DiskMgmt.FormatVolumeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:OneMMC.Views.DiskMgmt"
    xmlns:core="using:OneMMC.Core.Features.PCManagement.Models.DiskMgmt"
    xmlns:localization="using:OneMMC.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CloseButtonText="{x:Bind LocalizedStrings.Common_CancelButton}"
    PrimaryButtonText="{x:Bind LocalizedStrings.DiskMgmt_Format}"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ContentDialog.Resources>
        <localization:LocalizedStrings x:Key="LocalizedStrings" />
    </ContentDialog.Resources>

    <Grid MinWidth="450" MinHeight="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="{x:Bind LocalizedStrings.DiskMgmt_FormatVolume}"
            TextWrapping="Wrap" />

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="12">
                <InfoBar Severity="Warning" IsOpen="True" IsClosable="False" Message="{x:Bind LocalizedStrings.DiskMgmt_FormatWarning}" />

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_Drive}" FontWeight="SemiBold" />
                <TextBlock x:Name="DriveInfoTextBlock" />

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_VolumeLabel}" FontWeight="SemiBold" />
                <TextBox x:Name="VolumeLabelTextBox" MaxLength="32" />

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_FileSystem}" FontWeight="SemiBold" />
                <ComboBox x:Name="FileSystemComboBox" SelectedIndex="0">
                    <ComboBoxItem Content="NTFS" />
                    <ComboBoxItem Content="FAT32" />
                    <ComboBoxItem Content="exFAT" />
                </ComboBox>

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_AllocationUnitSize}" FontWeight="SemiBold" />
                <ComboBox x:Name="AllocationUnitComboBox" SelectedIndex="0">
                    <ComboBoxItem Content="{x:Bind LocalizedStrings.DiskMgmt_Default}" />
                    <ComboBoxItem Content="512 bytes" />
                    <ComboBoxItem Content="1024 bytes" />
                    <ComboBoxItem Content="2048 bytes" />
                    <ComboBoxItem Content="4096 bytes" />
                    <ComboBoxItem Content="8192 bytes" />
                    <ComboBoxItem Content="16 KB" />
                    <ComboBoxItem Content="32 KB" />
                    <ComboBoxItem Content="64 KB" />
                </ComboBox>

                <CheckBox x:Name="QuickFormatCheckBox" Content="{x:Bind LocalizedStrings.DiskMgmt_PerformQuickFormat}" IsChecked="True" />
                <CheckBox x:Name="EnableCompressionCheckBox" Content="{x:Bind LocalizedStrings.DiskMgmt_EnableCompression}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
