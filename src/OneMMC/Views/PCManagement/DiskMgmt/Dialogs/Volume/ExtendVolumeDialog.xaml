<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="OneMMC.Views.DiskMgmt.ExtendVolumeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:OneMMC.Views.DiskMgmt"
    xmlns:core="using:OneMMC.Core.Features.PCManagement.Models.DiskMgmt"
    xmlns:localization="using:OneMMC.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CloseButtonText="{x:Bind LocalizedStrings.Common_CancelButton}"
    PrimaryButtonText="{x:Bind LocalizedStrings.DiskMgmt_Extend}"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ContentDialog.Resources>
    </ContentDialog.Resources>

    <Grid MinWidth="400" MinHeight="250">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="{x:Bind LocalizedStrings.DiskMgmt_ExtendVolumeTitle}"
            TextWrapping="Wrap" />

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="12">
                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_MaximumAvailableSpace}" FontWeight="SemiBold" />
                <TextBlock x:Name="MaxExtendTextBlock" />

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_SelectedSpace}" FontWeight="SemiBold" />
                <NumberBox x:Name="ExtendSizeNumberBox" Minimum="1" SpinButtonPlacementMode="Inline" />
                
                <TextBlock x:Name="VolumeInfoTextBlock" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
