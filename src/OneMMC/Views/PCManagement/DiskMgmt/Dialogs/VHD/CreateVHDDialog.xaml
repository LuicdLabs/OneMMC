<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="OneMMC.Views.DiskMgmt.CreateVHDDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:OneMMC.Core.Features.PCManagement.Models.DiskMgmt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OneMMC.Views.DiskMgmt"
    xmlns:localization="using:OneMMC.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    CloseButtonText="{x:Bind LocalizedStrings.Common_CancelButton}"
    DefaultButton="Primary"
    PrimaryButtonText="{x:Bind LocalizedStrings.Common_CreateButton}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <localization:LocalizedStrings x:Key="LocalizedStrings" />
    </ContentDialog.Resources>

    <Grid MinWidth="450" MinHeight="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="{x:Bind LocalizedStrings.DiskMgmt_CreateVHD}"
            TextWrapping="Wrap" />

        <!--  Content  -->
        <StackPanel Grid.Row="1" Spacing="12">
            <TextBlock FontWeight="SemiBold" Text="{x:Bind LocalizedStrings.DiskMgmt_Location}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    x:Name="VHDPathTextBox"
                    Grid.Column="0"
                    Margin="0,0,8,0" />
                <Button
                    x:Name="BrowseVHDLocationButton"
                    Grid.Column="1"
                    MinWidth="120"
                    Click="BrowseButton_Click"
                    Content="{x:Bind LocalizedStrings.Common_BrowseButton}" />
            </Grid>

            <TextBlock FontWeight="SemiBold" Text="{x:Bind LocalizedStrings.DiskMgmt_VHDSize}" />
            <NumberBox
                x:Name="VHDSizeNumberBox"
                Maximum="2097152"
                Minimum="1"
                SpinButtonPlacementMode="Inline"
                Value="10240" />

            <TextBlock FontWeight="SemiBold" Text="{x:Bind LocalizedStrings.DiskMgmt_VHDType}" />
            <ComboBox x:Name="VHDTypeComboBox" SelectedIndex="0">
                <ComboBoxItem Content="{x:Bind LocalizedStrings.DiskMgmt_FixedSize}" />
                <ComboBoxItem Content="{x:Bind LocalizedStrings.DiskMgmt_DynamicallyExpanding}" />
            </ComboBox>
        </StackPanel>

    </Grid>
</ContentDialog>
