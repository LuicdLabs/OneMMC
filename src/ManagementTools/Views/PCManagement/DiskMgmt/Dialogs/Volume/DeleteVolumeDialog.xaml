<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ManagementTools.Views.DiskMgmt.DeleteVolumeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:ManagementTools.Views.DiskMgmt"
    xmlns:core="using:ManagementTools.Core.Features.PCManagement.Models.DiskMgmt"
    xmlns:localization="using:ManagementTools.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:converters="using:ManagementTools.Converters"
    CloseButtonText="{x:Bind LocalizedStrings.Common_CancelButton}"
    PrimaryButtonText="{x:Bind LocalizedStrings.Common_DeleteButton}"
    IsPrimaryButtonEnabled="{x:Bind ConfirmDeleteCheckBox.IsChecked, Converter={StaticResource NullableBoolToBoolConverter}, Mode=OneWay}"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ContentDialog.Resources>
        <localization:LocalizedStrings x:Key="LocalizedStrings" />
        <converters:NullableBoolToBoolConverter x:Key="NullableBoolToBoolConverter" />
    </ContentDialog.Resources>

    <Grid MinWidth="450" MinHeight="250">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="{x:Bind LocalizedStrings.DiskMgmt_DeleteVolumeTitle}"
            TextWrapping="Wrap" />

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="12">
                <InfoBar Severity="Error" IsOpen="True" IsClosable="False" Message="{x:Bind LocalizedStrings.DiskMgmt_DeleteVolumeWarning}" />

                <TextBlock Text="{x:Bind LocalizedStrings.DiskMgmt_Volume}" FontWeight="SemiBold" />
                <TextBlock x:Name="VolumeInfoTextBlock" />

                <CheckBox x:Name="ConfirmDeleteCheckBox" Content="{x:Bind LocalizedStrings.DiskMgmt_ConfirmDeleteVolume}" Margin="0,12,0,0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
