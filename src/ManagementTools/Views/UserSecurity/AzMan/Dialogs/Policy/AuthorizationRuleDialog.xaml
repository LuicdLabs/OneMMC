<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="ManagementTools.Views.UserSecurity.AzMan.Dialogs.AuthorizationRuleDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ManagementTools.Localization"
    mc:Ignorable="d">

    <Grid MinWidth="560" MinHeight="400" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_ScriptSourceCode_Label}" />

        <TextBox x:Name="ScriptSourceCodeTextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 IsReadOnly="True"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch" />

        <TextBlock Grid.Row="2" Text="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_ScriptPath_Label}" />

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ScriptPathTextBox"
                     Grid.Column="0" />
            <Button Grid.Column="1"
                    Click="OnBrowseScriptPathClick"
                    Content="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_Browse_Button}" />
        </Grid>

        <StackPanel Grid.Row="4" Spacing="6">
            <TextBlock Text="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_ScriptType_Label}" />
            <StackPanel Orientation="Vertical" Spacing="4">
                <RadioButton x:Name="VBScriptRadioButton" Content="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_VBScript}" GroupName="ScriptType" />
                <RadioButton x:Name="JScriptRadioButton" Content="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_JScript}" GroupName="ScriptType" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
            <Button x:Name="ReloadRuleButton"
                    Click="OnReloadRuleClick"
                    Content="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_ReloadRule_Button}" />
            <Button x:Name="ClearRuleButton"
                    Click="OnClearRuleClick"
                    Content="{x:Bind local:LocalizedStrings.Instance.AuthorizationRuleDialog_ClearRule_Button}" />
        </StackPanel>
    </Grid>
</UserControl>