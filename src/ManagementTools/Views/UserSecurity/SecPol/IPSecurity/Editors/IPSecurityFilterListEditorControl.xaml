<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="ManagementTools.Views.UserSecurity.SecPol.IPSecurity.Editors.IPSecurityFilterListEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid MinWidth="560" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar
            x:Name="ValidationInfoBar"
            IsClosable="True"
            IsOpen="False"
            Severity="Error" />

        <Grid
            Grid.Row="1"
            ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox
                x:Name="NameTextBox"
                Header="{x:Bind LocalizedStrings.IPSec_Column_Name}" />
            <TextBox
                x:Name="DescriptionTextBox"
                Grid.Column="1"
                Header="{x:Bind LocalizedStrings.IPSec_Column_Description}" />
        </Grid>

        <CommandBar
            Grid.Row="2"
            Background="Transparent"
            DefaultLabelPosition="Right">
            <AppBarButton
                Click="AddFilterButton_Click"
                Label="{x:Bind LocalizedStrings.Common_AddButton}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Add" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton
                Click="EditFilterButton_Click"
                Label="{x:Bind LocalizedStrings.Common_EditButton}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Edit" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton
                Click="DeleteFilterButton_Click"
                Label="{x:Bind LocalizedStrings.Common_DeleteButton}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Delete" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <ListView
            x:Name="FiltersListView"
            Grid.Row="3"
            AutomationProperties.Name="{x:Bind LocalizedStrings.IPSec_Editor_Filters}"
            ItemsSource="{x:Bind FilterItems}"
            MinHeight="260"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Text="{Binding Summary}"
                        TextWrapping="WrapWholeWords" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
