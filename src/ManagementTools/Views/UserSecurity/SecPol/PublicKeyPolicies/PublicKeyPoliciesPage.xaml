<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ManagementTools.Views.PublicKeyPoliciesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  CommandBar  -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Refresh" Label="Refresh" />
        </CommandBar>

        <!--  Content  -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="24,8,24,24" Spacing="4">
                <TextBlock
                    Margin="0,0,0,8"
                    FontWeight="SemiBold"
                    Text="Object Type" />

                <!--  Encrypting File System  -->
                <controls:SettingsCard Header="Encrypting File System">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE72E;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <!--  Data Protection  -->
                <controls:SettingsCard Header="Data Protection">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE72E;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <!--  BitLocker Drive Encryption  -->
                <controls:SettingsCard Header="BitLocker Drive Encryption">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE72E;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <!--  Certificate Services Client - Certificate Enrollment Policy  -->
                <controls:SettingsCard Header="Certificate Services Client - Certificate Enrollment Policy">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE7C3;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <!--  Certificate Path Validation Settings  -->
                <controls:SettingsCard Header="Certificate Path Validation Settings">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE7C3;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <!--  Certificate Services Client - Auto-Enrollment  -->
                <controls:SettingsCard Header="Certificate Services Client - Auto-Enrollment">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE7C3;" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
