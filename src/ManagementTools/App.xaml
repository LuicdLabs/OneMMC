<?xml version="1.0" encoding="utf-8" ?>
<Application
	x:Class="ManagementTools.App"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:ManagementTools"
	xmlns:converters="using:ManagementTools.Converters"
	xmlns:localization="using:ManagementTools.Localization">
<Application.Resources>
	<ResourceDictionary>
		<ResourceDictionary.MergedDictionaries>
			<XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
		</ResourceDictionary.MergedDictionaries>
		<!--  Converters  -->
		<converters:StringToBoolConverter x:Key="StringToBoolConverter" />
		<converters:NullToBoolConverter x:Key="NullToBoolConverter" />
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
		<converters:NullableBoolToBoolConverter x:Key="NullableBoolToBoolConverter" />
		<localization:LocalizedStrings x:Key="LocalizedStrings" />
        <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
		<!--  Other app resources here  -->
		<Style TargetType="StackPanel">
				<Setter Property="ChildrenTransitions">
					<Setter.Value>
						<TransitionCollection>
							<EntranceThemeTransition FromVerticalOffset="50" />
							<RepositionThemeTransition IsStaggeringEnabled="False" />
						</TransitionCollection>
					</Setter.Value>
				</Setter>
			</Style>
		<Style TargetType="ListView">
				<Setter Property="ItemContainerTransitions">
					<Setter.Value>
						<TransitionCollection>
							<EntranceThemeTransition FromVerticalOffset="50" />
							<RepositionThemeTransition IsStaggeringEnabled="False" />
						</TransitionCollection>
					</Setter.Value>
				</Setter>
		</Style>
		</ResourceDictionary>
	</Application.Resources>
</Application>
