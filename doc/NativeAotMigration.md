# Native AOT Migration Strategy

**Native AOT is the project's end-state goal.** Every new design and every rewrite must move the
codebase toward full AOT compatibility. This document is the plan of record: it maps each
AOT-incompatible pattern to its sanctioned AOT-compatible replacement and sequences the migration.
The measured baseline this plan starts from is recorded in
[NativeAotAssessment.md](NativeAotAssessment.md).

The default publish remains self-contained ReadyToRun **only as an interim state** until the
milestone gates below are met. It is a transition vehicle, not the destination.

## Terminology: WMI, MI, CIM, MMI (and which is which)

These names are used precisely throughout this document:

| Name | What it is | AOT status |
|---|---|---|
| **CIM** | The DMTF data-model standard (classes, instances, namespaces) that all of the below implement | n/a (a standard, not an API) |
| **Classic WMI COM API** | `IWbemLocator`/`IWbemServices` (wbemcli.h), the original WMI client API | Usable under AOT only via marshal-free COM (CsWin32 `allowMarshaling: false` or `[GeneratedComInterface]`) |
| **`System.Management`** | The original .NET wrapper over classic WMI COM (`ManagementObjectSearcher`) | **Not AOT/trim compatible** (official) |
| **MI API ("WMI v2")** | The native C API in `mi.h` (`MI_Application_*`, `MI_Session_*`), introduced in Windows 8/Server 2012; the latest version of the WMI technologies, CIM-standard based | Native C API — AOT-neutral, but has **no supported managed binding** (see next row) |
| **MMI — `Microsoft.Management.Infrastructure`** | The managed .NET binding of the MI API (`CimSession`, `CimInstance`); what PowerShell's CIM cmdlets use | **Confirmed NOT AOT compatible**: crashes with `MissingInteropDataException` in delegate marshalling (`MI_ApplicationWrapper_Initialize`, [PowerShell/MMI#54](https://github.com/PowerShell/MMI/issues/54)); package targets netstandard1.6 (cannot carry trim/AOT annotations); **repo archived 2024-06-14** — it will never be fixed |
| **WmiLight** | Third-party MIT-licensed WMI client (queries, methods, event subscriptions) over classic WMI with its own native shim | **Native AOT supported since 5.0** (`PublishWmiLightStaticallyLinked` can fold the shim into the exe) |

So: "the MI API" is the native `mi.h` C API, and MMI (`Microsoft.Management.Infrastructure`) is
its managed CIM wrapper — the one this repo already uses in the Windows Firewall feature. The
research verdict overturns the earlier "uncertain" rating: **MMI is a dead end for AOT**, and the
existing 467 MMI call sites must migrate too.

## Sanctioned replacements (normative)

New code MUST use the right-hand column. Existing code migrates per the phase plan.

| Forbidden pattern (AOT-incompatible) | Sanctioned AOT-compatible replacement |
|---|---|
| `dynamic` over COM (IDispatch late binding) | Typed COM interfaces via `[GeneratedComInterface]`/`ComWrappers` source generation; `ComVariant` (`System.Runtime.InteropServices.Marshalling`) for VARIANT parameters |
| `Type.GetTypeFromProgID` / `GetTypeFromCLSID` + `Activator.CreateInstance` | `CLSIDFromProgID` + `CoCreateInstance` P/Invoke (CsWin32) returning a `[GeneratedComInterface]` pointer via `ComWrappers` |
| Handwritten `[ComImport]` interfaces | `[GeneratedComInterface]` (source-generated, marshal-free) |
| `System.Management` (`ManagementObjectSearcher`, …) | **WmiLight** (primary), or classic WMI COM (`IWbemServices`) via CsWin32 `allowMarshaling: false` (fallback, no third-party dependency) |
| `Microsoft.Management.Infrastructure` (`CimSession`, …) | Same as above — WmiLight covers queries, method invocation, and event subscriptions over WQL |
| `System.DirectoryServices(.AccountManagement)` | NetAPI32 (`NetUserEnum`, `NetLocalGroup*`, `NetUserGetInfo`, …) and LSA via CsWin32 |
| `System.Diagnostics.PerformanceCounter` | PDH API (`PdhOpenQuery`, `PdhCollectQueryData`, …) via CsWin32 |
| `{Binding}` in XAML | `{x:Bind}` (compile-time generated) |
| Reflection-based `JsonSerializer` calls | Source-generated `JsonSerializerContext` |
| `[ObservableProperty]` on fields (MVVMTK0045) | `[ObservableProperty]` on partial properties (MVVM Toolkit 8.4+, `LangVersion=preview` already enables this) |
| Non-`partial` classes crossing the WinRT ABI (CsWinRT1028/1029) | Mark the class (and containing types) `partial` |

Engineering notes:

- **CsWin32 for AOT**: set `"allowMarshaling": false` in `NativeMethods.json` so generated code
  avoids the runtime marshaler (documented CsWin32 support for `PublishAot`/`PublishTrimmed`/
  `DisableRuntimeMarshalling` environments).
- **COM automation servers used by this app expose dual interfaces** (AzRoles `IAzAuthorizationStore`,
  COMAdmin catalog, `HNetCfg` firewall, Task Scheduler `ITaskService`), so vtable-based
  `[GeneratedComInterface]` calls replace IDispatch dispatch one-for-one; `dynamic` is never needed.
- **WmiLight deployment**: OneMMC.Core is a class library, so the app project must add
  `<TrimmerRootAssembly Include="WmiLight" />` (documented WmiLight requirement for .NET Standard/
  library encapsulation under AOT) and can set `<PublishWmiLightStaticallyLinked>true</...>` to
  avoid shipping `WmiLight.dll`.
- **WmiLight risk**: single-maintainer project (MIT, 52 releases, actively maintained). Mitigation:
  the MIT license permits forking/vendoring, and the fallback path (classic WMI COM via CsWin32)
  removes the dependency entirely at the cost of more in-house interop code.
- **XAML metadata**: until the XAML compiler/CsWinRT automate rooting, `{Binding}`-free XAML plus
  `partial` classes is the reliable path; use `TrimmerRootDescriptor` only as a last resort for
  types that are genuinely only reachable dynamically.

## Phase plan

Each phase has an acceptance gate measured with the existing evaluation infrastructure
(`/p:OneMMCAotAnalysis=true`; see NativeAotAssessment.md for the exact commands).

| Phase | Scope | Gate |
|---|---|---|
| **M0 — hygiene** (start immediately, no dependencies) | Mark the 74 CsWinRT1028 classes `partial`; convert 26 MVVMTK0045 sites to partial properties; add `JsonSerializerContext` for the 7 JSON sites; convert `{Binding}` → `{x:Bind}` (~925 sites, feature-by-feature starting with WF and AzMan dialogs) | Run B analyzer build: 0 × CsWinRT1028/1029, 0 × MVVMTK0045, 0 × IL2026/IL3050 from JSON; `{Binding}` count trending to 0. **Status 2026-07-02: partial classes, partial properties, and JSON contexts are DONE and gate-verified (analyzer warnings 1,144 → 1,030; CsWinRT1028 = 0, MVVMTK0045 = 0). WF `{x:Bind}` conversion DONE (754 → 0; localized strings bind statically via `{x:Bind prefix:LocalizedStrings.Instance.KEY}`, item templates use `x:DataType`, item classes hoisted to namespace level where needed). Repo-wide `{Binding}` conversion DONE — the earlier "1,240 remaining" figure had included stale `obj/` build copies; the true source-tree residual was 169 sites in 28 files, now **0**. Non-WF views reuse the WF patterns; VM-backed dialogs bind `{x:Bind _viewModel.X, Mode=OneWay/TwoWay}`; `GroupPolicyTreeItem`, `RSoPTreeItem`, `Breadcrumb`, `DesignatedFileTypeItem`, `ThemeOption` hoisted to namespace scope; template→page-VM `Command` bindings became `Click` + `Tag="{x:Bind}"` handlers (LusrMgr). All reflection property paths also removed: XAML `DisplayMemberPath`/`SelectedValuePath` (WMC1510 12 → 0) and runtime `DisplayMemberPath` assignments (PerfMon, SoftwareRestriction) replaced with `ItemTemplate`s or `ToString()`. 37 orphaned page-level `<LocalizedStrings>` resources and the dead `GpoBrowseDialog` view/viewmodel (empty code-behind, superseded by the DeployPrinterDialog GPO flyout) deleted. Gate re-verified 2026-07-02 (Release x64 full rebuild, `OneMMCAotAnalysis=true`): 0 errors; CsWinRT1028/1029 = 0, MVVMTK0045 = 0, WMC1510 = 0; the remaining 1,074 warnings are all IL2026/IL3050-family from the M2–M4 backlog (`System.Management`, MMI, COM/`dynamic`) — none XAML-side. M0 is complete.** |
| **M1 — boot under AOT** | With M0 done, root remaining XAML metadata; fix startup-path warnings; re-run the Run D publish and launch | AOT-published app boots to the main window and navigates all top-level pages. **Status 2026-07-03: M1 gate PASSED — no additional rooting or startup fixes were needed; M0's partial classes + `{x:Bind}` conversion alone eliminated the `0xC000027B` startup crash.** Run D re-publish (Release x64, ILC + link OK): payload 73.8 MB / 14 files. AOT exe boots to the main window (Serilog confirms App init → OnLaunched → MainWindow init → first navigation), and a UIA smoke run (`eng/Drive-AotSmokeNavigation.ps1`, new — reusable for M2–M4 feature smoke tests) selected all six top-level categories + Settings: every page fully rendered (localized strings, theme setting, live PrintManagement data), process alive and responsive throughout, no exceptions in the app log. The GC-race navigation hang (dotnet/runtime#104582) did not manifest. Distinct warning sites 2,381 → 2,249 (`eng/aot-logs/m1-gate-summary.md`); 0 XAML-side — the residual is the M2–M4 backlog: 1,221 inside dependency packages (`System.Management`, MMI, `System.DirectoryServices*`, RuntimeBinder) and 926 in AzMan `dynamic`. Startup-path code audit (DI bootstrap, `AppSettings` JSON, App.xaml, MainWindow, PCManagement hub) found everything already explicit/source-generated. |
| **M2 — WMI/CIM migration** | Replace `System.Management` (121 sites: DiskMgmt, DevMgmt, TPM, WindowsServices, ComExp, WF monitoring) and MMI (467 sites: WF) with WmiLight (or CsWin32 IWbem fallback); remove both package references | Grep: 0 `using System.Management`, 0 `Microsoft.Management.Infrastructure`; affected features pass manual smoke tests on non-AOT build first, then under AOT. **Status 2026-07-03 (in progress): WmiLight 7.2.0 wired** (central version + Core reference + `TrimmerRootAssembly` rooting in the app project; `WmiLight.Native.dll` deploys, managed assembly folds into the AOT exe). **Migrated and smoke-tested on Debug build: TPM, WindowsServices (incl. `Change` method call), DevMgmt (4 queries), WF rule-change watcher (`WmiEventWatcher`, all 16 classes start), ComExp DTC (embedded out-param objects as `WmiObject`/`WmiObject[]`).** WmiLight-under-AOT validated end-to-end: AOT-published exe renders the TPM page with live WMI data. Shared plumbing added under `Core/Infrastructure/Wmi/`: `WmiObjectDisposalExtensions.DisposeItems` (prompt native-handle release, mirrors the System.Management helper) and `DmtfDateTimeConverter` (WmiLight returns CIM datetimes as raw DMTF strings). **Update 2026-07-03 (later): the `System.Management` half of M2 is COMPLETE** — DiskMgmt (all 5 files, queries + every Storage Management method call) migrated; legacy disposal helper and the `System.Management` package reference deleted; grep gate met (0 `using System.Management`). Two more shared helpers landed: `WmiPropertyExtensions.GetPropertySafe` (consolidates 4 duplicated per-file property readers) and `WmiMethodParameterExtensions` — **required reading for the MMI migration**: WMI providers demand VT_I4 for CIM uint16/uint32 method parameters, VT_BSTR for uint64, VT_I2 for char16; WmiLight's native VT_UI2/VT_UI8 encodings are rejected with `WBEM_E_TYPE_MISMATCH` (0x80041005), caught live on `MSFT_Disk.Initialize`. Destructive paths verified against a disposable 100 MB VHD (Initialize GPT, CreateSimpleVolume → auto letter + NTFS format, GetSupportedSize, real 10 MB shrink, SizeMin guard). **Remaining: MMI → WF (467 sites, 14 files); final AOT smoke pass; then remove the `Microsoft.Management.Infrastructure` package.**

**BLOCKING FINDING 2026-07-03 — WmiLight cannot replace MMI for Windows Firewall.** The plan's premise ("WmiLight covers … method invocation") holds only for read/query/associators/delete/method. But the WF write path is instance-CRUD: verified operation matrix across the 14 files is 15 × `ModifyInstance`, 4 × `CreateInstance`, 3 × `DeleteInstance`, plus building instances from scratch (`new CimInstance` ×13 with typed `CimProperty.Create`, `CimFlags.Key`, and **embedded instance arrays** — `CimType.InstanceArray` for IKE Proposals). WmiLight's entire public surface (WmiConnection + extensions, verified against upstream source) has **zero** `PutInstance`/`CreateInstance`/`ModifyInstance`/`SpawnInstance` — it supports only WQL query, `DeleteInstance(WmiObject)`, `ExecuteMethod`, and events. The WF read/modify/write pattern (`RuleApplier`, `CimSetManager`, `WindowsFirewallProfileService`) reads an enumerated/associated `CimInstance`, mutates typed properties, and `ModifyInstance`es it — read and write coupled through one CIM object, so it can't be split "read via WmiLight, write via something else." The sanctioned fallback (classic WMI COM `IWbemServices.PutInstance` via CsWin32 `allowMarshaling:false`) is the correct end-state but is the single largest/riskiest interop task in the migration: marshal-free `IWbemLocator`/`IWbemServices`/`IWbemClassObject`/`IEnumWbemClassObject`, VARIANT+CIMTYPE property get/put, SAFEARRAY including **embedded `IWbemClassObject` arrays**, key qualifiers, and PutInstance CREATE-vs-UPDATE flags — mutating live firewall/IPsec config (worst failure mode for an admin tool). It belongs with M3's COM-interop work, not as a WmiLight swap. **MMI staying in WF does not block AOT boot** (M1 passes; MMI is only reached at runtime when firewall write ops execute), so isolating MMI to WF is a viable documented interim while the IWbem/CIM layer is scoped as its own effort.

**Decision 2026-07-03: the `System.Management` half of M2 is DONE and gate-verified; MMI-in-WF is a documented, isolated interim.** The firewall IWbemServices/CIM `PutInstance` layer is deferred to a dedicated effort sequenced with M3 (it is COM-interop work, structurally identical to the `[GeneratedComInterface]` rewrites there). M2's first grep gate (`0 using System.Management`) is met; the second (`0 Microsoft.Management.Infrastructure`) is intentionally deferred with WF. Proceeding to M3.

**Update 2026-07-04: WF Half B (MMI → IWbemServices) foundation approach VALIDATED and scoped** (see [[onemmc-wf-iwbem-halfb-design]]). This is the ONLY remaining AOT interop task after M3 completed — ~489 MMI (`Microsoft.Management.Infrastructure`) sites in 16 WF files (ConnectionSecurity/Profiles/Monitoring instance CRUD). **Approach settled: use CsWin32's marshal-free IWbem interfaces, NOT hand-authored `[GeneratedComInterface]`.** Empirically validated: adding `IWbemLocator`/`IWbemServices`/`IWbemClassObject`/`IEnumWbemClassObject` to Core's `NativeMethods.txt` (`allowMarshaling:false` already set) builds clean and emits AOT-compatible function-pointer-vtable structs (`IWbemClassObject*` + `lpVtbl[N]` unmanaged calls) with **vtable order correct from Windows metadata** — critical because these interfaces live in `wbemcli.h`, not a type library, so `eng/Dump-TypeLibVtable.ps1` can't dump them and hand-authoring would risk silent vtable-order corruption. The remaining work (a managed `CimInstance`-like wrapper over `IWbemClassObject*`, the read path, then the write path incl. **embedded `IWbemClassObject` arrays** for IKE Proposals, then the 489-site sweep) is the migration's single largest/riskiest task — it mutates live firewall/IPsec config, so writes must be verified against a disposable test rule/set, never production. Kept as its own dedicated effort; clears M2's second gate (`0 Microsoft.Management.Infrastructure`), then the package ref is removed.

**Update 2026-07-04 (later): WF Half B read-only foundation LANDED and read-parity-verified vs MMI.** Added the six IWbem types to `NativeMethods.txt` (CsWin32 emits function-pointer-vtable structs, vtable order from Windows metadata, builds 0/0) and a marshal-free wrapper layer `Features/SystemManagement/Infrastructure/WF/Wbem/` — the read half of the MMI replacement: **`WbemServices`** (the `CimSession` equivalent: `CoCreateInstance(IWbemLocator)` → `ConnectServer(root\StandardCimv2)` → **`CoSetProxyBlanket`** [handwritten import — CsWin32 doesn't project it; without the DCOM blanket every call fails `WBEM_E_ACCESS_DENIED`, which MMI set internally], then lazy `EnumerateInstances` via `CreateInstanceEnum`+`Next` and `EnumerateAssociatedInstances` via `ExecQuery "ASSOCIATORS OF …"`); **`WbemObject`** (the `CimInstance` equivalent over `IWbemClassObject*`: `GetValue` boxes a returned VARIANT to the **exact CLR type the old `CimInstance` produced, driven by the property's CIMTYPE** — WMI stores both `CIM_UINT16` and `CIM_UINT32` as `VT_I4`, so VARTYPE alone can't distinguish `ushort` from `uint`, and the WF consumers pattern-match on that type e.g. `value switch { 1 => … }`; handles scalars, string arrays, fixed-width numeric arrays via raw SAFEARRAY data, and embedded objects/object-arrays [IKE Proposals] as further `WbemObject`s; property `Get` is a **non-throwing vtable-slot-4 call** so an absent name degrades to `null` like the `CimInstance` indexer, no first-chance `COMException` — same PreserveSig principle as the AzMan fix); **`WbemCimType`** (CIMTYPE + WBEM flag constants). `Variant` gained a `RawValue` accessor. **Verified live READ-ONLY, side by side against MMI** (scratch `WbemHarness`): 43 real firewall instances (3 profiles + 40 rules), **1,626 properties read through BOTH stacks — 0 value mismatches AND 0 CLR-type diffs** (the CIMTYPE boxing reproduces MMI's types exactly), `ASSOCIATORS OF` matched security filters on 60/60 rules. Core + full app build 0/0. **Remaining (write path — mutates live config, verify against a disposable test rule/set NEVER production):** `SpawnInstance` + `Put` + `PutInstance` CREATE/UPDATE + `DeleteInstance` + the one `ExecMethod`, embedded `IWbemClassObject` array **writes** for Proposals, then the 16-file coordinated conversion (`CimSession`→`WbemServices`, `CimInstance`→`WbemObject`, `CimProperty.Create`/`CimType`/`CimFlags`→wrapper equivalents), then remove the `Microsoft.Management.Infrastructure` package ref → M2 second gate (`0 Microsoft.Management.Infrastructure`).<br><br>**Update 2026-07-09: WF Half B WRITE path LANDED and round-trip-verified vs MMI (c527d75).** The wrapper gained the full instance-CRUD write surface, all marshal-free: `WbemServices.SpawnInstance` (GetObject class + `SpawnInstance`), `CreateInstance` (`PutInstance` CREATE_ONLY), `ModifyInstance` (`PutInstance` UPDATE_ONLY), `DeleteInstance` (by `__PATH`); `WbemObject.SetProperty(name,value,WbemType)`/`SetProperty(name,value)` with type inference → `BuildVariant` (String/Boolean, uint16|32→VT_I4, uint64→VT_BSTR, StringArray, InstanceArray) → non-throwing `Put` (vtable slot 5, `Type=0` so WMI coerces to the class-declared CIMTYPE); `WbemCimType` write flags; `Variant.FromBool` + `FromComInterfaceArray` (embedded IKE Proposals as a `VT_ARRAY|VT_UNKNOWN` SAFEARRAY of `IWbemClassObject*`). **Bug found & fixed:** `FromComInterfaceArray` must populate the VT_UNKNOWN SAFEARRAY via `SafeArrayAccessData` + a direct pointer write + `Marshal.AddRef`, **not** `SafeArrayPutElement` — the latter's VT_UNKNOWN path (walking the element vtable to AddRef) **hard-faults the process** under the source-generated/marshal-free interop model (isolated with plain `GetIUnknownForObject` pointers, so it is not WMI-specific); the direct write does the same work and is stable, and `FADF_UNKNOWN` (set by `SafeArrayCreateVector`) still makes `VariantClear`/`SafeArrayDestroy` Release each element so the ref count stays balanced. **Verified live, elevated, against a DISPOSABLE `MSFT_NetIKEMMCryptoSet`** (unique-GUID `CreationClassName`, self-cleaning at startup + in `finally`) — **never production config**: full round-trip cross-checked against MMI (independent stack) — create with 2 embedded proposals → MMI + wrapper both read the set and both proposals back → modify (`MaxLifetimeMinutes`) confirmed by MMI → delete confirmed gone (11/11 checks PASS). A differential MMI probe drove the disposable set to a provider-accepted algorithm combo (the recurring "main mode key exchange algorithm is invalid" was **shared bad test data**, identical on both stacks — proving the wrapper reaches the provider and behaves exactly like MMI, not a wrapper defect). The temporary `[assembly:InternalsVisibleTo]` harness shim was removed; Core + full app build 0/0. **Remaining for the M2 second gate: the 16-file coordinated conversion + package-ref removal (unchanged).**<br><br>**Update 2026-07-09 (later): WF converted off MMI — M2 SECOND GATE MET, M2 COMPLETE (a3ff770).** All 14 WF service files (ConnectionSecurity: CimHelper/ValueHelper/RuleMapper/RuleApplier/AuthManager/ConnectionSecurityService; Profiles: AuthProposalManager/ProposalConverter/CimSetManager/InternalTypes/WindowsFirewallProfileService; Monitoring: FirewallMonitoringService; Rules: WindowsFirewallRuleService/ComApplier) migrated from `Microsoft.Management.Infrastructure` to the marshal-free `WbemServices`/`WbemObject` wrapper, and the **`Microsoft.Management.Infrastructure` package reference removed** (Core.csproj + Directory.Packages.props + ThirdPartyNotices). Uniform mapping: `CimSession.Create(null)`→`WbemServices.Connect`, `EnumerateInstances(ns,cls)`→`EnumerateInstances(cls)`, `EnumerateAssociatedInstances(ns,…)`→`(inst,assoc,result)`, `CimInstanceProperties[n]?.Value`→`GetValue(n)`, `…[n].Value=v`→`SetProperty(n,v[,WbemType])`, `new CimInstance(cls,ns)`→`SpawnInstance(cls)` (**session threaded into the proposal/skeleton builders** — MMI could construct a client-side instance without a connection; `SpawnInstance` needs one to `GetObject` the class), `Create/Modify/DeleteInstance`+`InvokeMethod`→wrapper equivalents, `CimException`→`COMException`, `CimSystemProperties.ClassName`→`WbemObject.ClassName`. Wrapper gained `ExecMethod` (the one `InvokeMethod`: con-sec Enable/Disable), `TrySetProperty`/`HasProperty` (the "set only if present" guard for TunnelType/OverrideBlockRules), `SetProperty(name,null)`→VT_NULL clear (+`Variant.Null`), and a **finalizer backstop** on `WbemObject` (the services enumerate many objects through lazy LINQ that can't always dispose each deterministically — mirrors the old `CimInstance` finalizer; `IWbemClassObject` is a free-threaded data object so finalizer-thread release is safe). `UpsertSet`/`UpsertMainModeSet` became `void` (their created instances were only ever disposed). **Verified live (non-AOT Debug, elevated):** Core + full app build 0/0; read smoke through the converted public services (3 profiles w/ real log paths, IPsec defaults/settings snapshot, tunnel-auth lists, Main/Quick-Mode SA enumerations); and a **disposable connection-security rule write round-trip** (unique name, deleted in `finally`, never production) — AddRule (SpawnInstance+typed Put+CreateInstance+ApplyRuleState+address/protocol/interface filters via ASSOCIATORS OF+Put+ModifyInstance) → read back (InboundSecurity=Request, Enabled=true) → SetRuleEnabled(false) via ExecMethod Disable → DeleteInstance, all PASS. **Both M2 grep gates now met (`0 using System.Management`, `0 Microsoft.Management.Infrastructure` as a code/package dependency — the residual text hits are XML-doc comments documenting the CIM equivalents). M2 is COMPLETE; only M4 (LusrMgr→NetAPI32, PerfMon→PDH, drop `System.DirectoryServices*`/`PerformanceCounter`, flip default publish to `PublishAot`) remains.** |
| **M3 — COM/`dynamic` rewrite** | AzMan (~150 `dynamic` + ProgID), ComExp, WF `HNetCfg`, TaskSchd, shared `DirectoryObjectPickerService` → `[GeneratedComInterface]` + `CoCreateInstance`; delete `ComPropertyAccessor` | Grep: 0 `dynamic` in Core/App, 0 `GetTypeFromProgID/CLSID`, 0 `[ComImport]`; features smoke-tested under AOT. **Status 2026-07-03: TaskSchd pilot DONE and the source-gen COM pattern is proven.** Established the reusable foundation in `Core/Infrastructure/Interop/`: `ComActivator` (`CoCreateInstance` via CsWin32 `allowMarshaling:false` + `StrategyBasedComWrappers`, replacing `GetTypeFromCLSID`+`Activator.CreateInstance`; `Release` disposes the wrapper), `IDispatch` (source-gen base reproducing the dual vtable `IUnknown[3]+IDispatch[4]+members`), and a blittable 24-byte `Variant` (VT_EMPTY/ERROR/I4/BSTR — chosen over `ComVariant` to avoid the assembly-wide `[DisableRuntimeMarshalling]`, which would first require converting Core's 3 remaining runtime-marshalled handwritten P/Invoke files — SecPol/DevMgmt/AclEditor — to `[LibraryImport]`). TaskSchd's 7 `[ComImport,InterfaceIsDual]` interfaces → `[GeneratedComInterface] : IDispatch` with explicit `get_`/`put_` accessors (exact vtable order), `Variant` params, raw-`short` VARIANT_BOOL, `nint` placeholders for uncalled members. Core `NativeMethods` gained `CoCreateInstance` + `allowMarshaling:false` (audited safe — Core consumes no CsWin32 COM RCWs). **Verified live on Debug** (console harness: activation+Connect, folder recursion, task enumeration, throwaway-task register/enable/run/export/delete round-trip — all correct) and **under Native AOT** (Run D publish + Task Scheduler page smoke). **Reusable patterns for the rest of M3** (see [[onemmc-m3-source-gen-com-pattern]]): dual interfaces need the `IDispatch` base + explicit accessors; VARIANT → blittable `Variant`; `dynamic`/IDispatch late binding → typed `[GeneratedComInterface]` vtable calls. **Update 2026-07-03 (later): GpEdit DONE.** `IGroupPolicyObject` (pure IUnknown, non-dual — no IDispatch base) → `[GeneratedComInterface]`; the `[ComImport]` coclass → CLSID + `ComActivator`. Source-gen doesn't marshal `StringBuilder`, so fill-buffer params became `nint` (pinned wide-char buffer at the 2 call sites; placeholders for the rest); `[MarshalAs(LPStruct)] Guid` → `in Guid`; `[MarshalAs(Bool)] bool` and `[MarshalAs(LPWStr)] string` kept as-is. Verified live via an **STA** console harness (open local machine GPO, read DisplayName/FileSysPath/registry key — all correct). **New learning: coclass ThreadingModel matters** — GroupPolicyObject is Apartment-model (STA-affine), so raw `CoCreateInstance` must run on an STA thread (Task Scheduler's coclass is Both/Free, so its harness worked MTA). The `ComActivator` itself is apartment-agnostic; callers on STA-affine coclasses must be on an STA thread (GpEdit is only reached from the app's STA UI thread). **Update 2026-07-03 (later): ComExp DONE.** `ComponentServicesManager`'s COMAdmin late binding (5 `dynamic` sites) + 2 ProgID-reflection activations → typed dual `[GeneratedComInterface]`s (`ICOMAdminCatalog`/`ICatalogCollection`/`ICatalogObject`, minimal read-only surface) activated via `ComActivator` with the registry-verified CLSID (`COMAdmin.COMAdminCatalog` → `{F618C514-DFB8-11D1-A2CF-00805FC79235}`, ThreadingModel=Both — the existing `Task.Run` MTA threads are fine, no STA executor needed). **Vtable order was sourced from the machine's own type library, not IDL mirrors** — new reusable dumper `eng/Dump-TypeLibVtable.ps1` (`LoadTypeLibEx` + `ITypeInfo.GetFuncDesc` slot dump; dual-interface members start at slot 7) — use it for the remaining azroles/SecPol interfaces. Enumeration switched from `IEnumVARIANT` `foreach` to `Populate` + `get_Count`/`get_Item` indexing (no enumerator interop), and the per-instance nested re-enumeration became one up-front appId dictionary. `Variant` gained its read half for VARIANT-returning getters: VARTYPE constants, `ToInvariantString()` (preserving the old `Convert.ToString(..., InvariantCulture)` semantics incl. `"True"/"False"` for VT_BOOL), and `Clear()`/`Dispose()` via a `VariantClear` `[LibraryImport]` so returned BSTR/object variants are freed; the reflection `Value` reader + missing-property cache were deleted. IDL retvals typed `IDispatch*` (GetCollection/Item) are declared as the concrete generated interface — the identity QI happens at the wrapper cast. **Verified live** (console harness driving the production `GetComPlusApplicationsAsync`/`GetComPlusRunningProcessesAsync`): 6 COM+ applications with GUID IDs and correct Library/Server activation mapping, plus a live `ApplicationInstances` hit (System Application in dllhost.exe, `RunForever` VT_BOOL read correctly). **Update 2026-07-04: AzMan DONE — the flagship.** The whole service (~150 `dynamic` sites across 11 files + 5 ProgID-reflection activations + the reflection `ComPropertyAccessor` + `ComObjectScope`) → 13 typed dual `[GeneratedComInterface]`s in `Native/AzRolesNative.cs` (`IAzAuthorizationStore3`, `IAzApplication`, `IAzScope`, `IAzApplicationGroup2`, `IAzRole`, `IAzTask`, `IAzOperation`, and the six collections) transcribed member-for-member from the machine's own `azroles.dll` type library via `eng/Dump-TypeLibVtable.ps1` (enhanced this pass to decode parameter/return types, following PTR/SAFEARRAY/USERDEFINED — so signatures, not just slot order, come from the binary). `Native/AzRolesCom.cs` centralizes activation (`ComActivator` + registry-verified CLSID `{B2BCFF59-…}`, ThreadingModel=Both — but the service keeps its existing dedicated STA executor, so all COM runs STA regardless), release, LONG↔bool, and one-based collection enumeration (`Item(0)` fails `E_INVALIDARG`, `Item(1)` is the first element — verified live). **AzMan-specific facts that bit and were fixed** (all typelib/live-verified): (1) AzRoles booleans (`GenerateAudits`/`Writable`/`IsRoleDefinition`/`ApplyStoreSacl`) are **LONG (I4)**, not VARIANT_BOOL; (2) nearly every mutator has a trailing `[in,optional] VARIANT varReserved` and `Submit(lFlags, varReserved)` — late binding filled these, typed callers pass `Variant.Missing`/`0`; (3) "array of strings" getters (`Members`/`Operations`/`PolicyAdministratorsName`/…) return a VARIANT wrapping a **SAFEARRAY** (of VARIANT or BSTR) — new `Variant.ToStringList()`; (4) a **parent must be `Submit()`ted before children can be created on it** (else `0x80072089` "parent uninstantiated"), so the export/import tree-copy submits each object before adding members. `Variant` gained `ToStringList()`, `ToComInterface<T>()` (wrap a returned `VT_DISPATCH/UNKNOWN` reference as a UniqueInstance wrapper), and SAFEARRAY free via `VariantClear`. The reader's per-property degrade-on-failure behavior is preserved by `SafeString/SafeBool/SafeInt/SafeStringList`. **Verified live** (console harness on throwaway `msxml://` stores, 27/27 checks): create+populate (app/operation/task/role-definition/role-assignment+SID/basic-group+member/scope/scope-task), read-back (enumeration + LONG booleans + SAFEARRAY member/operation/task lists), export the full tree → reopen → verify, import an application into a fresh store, delete. Full app builds 0/0. **Refinement 2026-07-04: PreserveSig on the SAFEARRAY getters kills first-chance-exception noise.** The reported recurring `COMException 0x80070032` ("[AzManService] COM string-array read failed: The request is not supported") was a *behavioral* regression from the conversion, not a functional one: the original reflection reader called these array getters via `IDispatch::Invoke` (which returns the HRESULT), whereas the typed `void get_X(out Variant)` throws on any failure HRESULT. AzMan does not support `PolicyAdministratorsName`/`PolicyReadersName`/`DelegatedPolicyUsersName` on **XML (`msxml://`) stores** (returns `ERROR_NOT_SUPPORTED` = `0x80070032`), so every store/application read threw + caught a first-chance COMException. Fix: declared the 11 VARIANT-array getters `[PreserveSig] int` (ABI-identical to `void` — same `HRESULT get_X(VARIANT*)` slot, but the HRESULT surfaces as a return value instead of a throw) and rewrote `SafeStringList`/`CopyStringList` to `hr >= 0 ? value.ToStringList() : []` with no try/catch. Re-verified with the harness (`+FirstChanceException` counter): 27/27 round-trip checks still pass **and 0 first-chance `COMException 0x80070032`**. Full app builds 0/0. **Update 2026-07-04: SecPol was a false positive — NOT a COM target.** The "SecPol (`dynamic`)" line came from a naive `\bdynamic\b` grep: `SecurityOptionsPolicyProvider`'s 20 hits were all comments plus a `SecurityPolicyDefinition` **parameter named `dynamic`** (a contextual-keyword identifier, strongly typed, zero AOT impact) — the whole SecPol folder has **no** `[ComImport]`/`GetTypeFromProgID`/`Activator.CreateInstance`/`IDispatch`/`Marshal.ReleaseComObject` (it's registry + P/Invoke + source-gen JSON only). Renamed the identifier to `dynamicDef` and reworded the bare-"dynamic" comments to "sceregvl.inf" (the precise meaning), so SecPol is now `\bdynamic\b`-clean; no behavior change. **Lesson for the gate: "0 `dynamic`" means the late-binding *keyword*, not the word** — legitimate domain prose keeps it (DiskMgmt "dynamic disk", CryptUi "dynamic [library] loading"); verify hits are code, not comments/identifiers. **Update 2026-07-04: DirectoryObjectPicker DONE.** `IDsObjectPicker` (`[ComImport]`, pure IUnknown/non-dual) + the minimal `IDataObject` surface OneMMC uses (only `GetData`) → `[GeneratedComInterface]`s; `GetTypeFromCLSID`+`Activator` → `ComActivator`; `Marshal.ReleaseComObject` → `ComActivator.Release`. The public `ShowDialog` API + ~20 callers are unchanged (the "blast radius" was callers of the public method, which didn't move). Also made the whole file AOT-clean: the blittable-struct `Marshal.SizeOf`/`StructureToPtr`/`PtrToStructure`/`ReadInt32` (`[RequiresDynamicCode]`) → `Unsafe.SizeOf`/`Span` write/`Unsafe.Read`; the fetched-`objectSid` `Marshal.GetObjectForNativeVariant` → new `Variant.ToByteArray()` (VT_ARRAY|VT_UI1 SAFEARRAY via `SafeArrayAccessData`). **Two source-generator constraints learned:** (1) the BCL `FORMATETC`/`STGMEDIUM` can't be used in a generated signature — `FORMATETC` has `[MarshalAs]` on its enum fields, and a `TYMED` **enum** field trips **SYSLIB1051** as an `out` param — so use a blittable `FORMATETC_Native` and carry `tymed` as a plain `uint` (reinterpret to the CsWin32 `STGMEDIUM` for `ReleaseStgMedium`); (2) nested types in generated COM signatures must be `internal`+ (**SYSLIB1090**), not `private`. Verified live (standalone harness; coclass ThreadingModel=Both): `CoCreateInstance(DsObjectPicker)` S_OK → ComWrappers QI to the source-gen `IDsObjectPicker` OK → `Initialize` with a real one-scope `DSOP_INIT_INFO` returns S_OK (proves activation + vtable slot + DSOP struct marshalling); `InvokeDialog`/`GetData`/`ExtractSelections` are modal-dialog-only (not headless-testable) and rest on the proven foundation + the standard `IDataObject::GetData` slot. Core + full app build 0/0. **Update 2026-07-04: WF HNetCfg (Half A) DONE — M3 IS COMPLETE.** The firewall rule/policy COM (`WF/ComInterfaces.cs`: 5 `[ComImport, InterfaceIsDual]` — `INetFwPolicy2`, `INetFwRules`, `INetFwRule`/`2`/`3`) → `[GeneratedComInterface] : IDispatch` (explicit `get_`/`put_` for rules, `get_`/`set_` for the policy's parameterized properties; vtable order from `FirewallAPI.dll`'s typelib; **interface inheritance** so `INetFwRule2/3` IS-A their bases → correct QI on `Add`). The 2 `HNetCfg` ProgID activations (`WindowsFirewallSupport.CreatePolicy2/CreateRule`) → `FirewallCom` (`ComActivator` + registry CLSIDs, both ThreadingModel=Both). New reusable bits: a source-gen **`IEnumVARIANT`** (the rule collection has no index accessor — `_NewEnum` → `Next` → `Variant.ToComInterface`), and `Variant.FromStringArray` (VT_ARRAY|VT_BSTR SAFEARRAY via `SafeArrayCreateVector`/`PutElement`) for the `Interfaces`/`ExcludedInterfaces` VARIANT. NET_FW_* enums = I4 (`int`), VARIANT_BOOL = raw `short`. Consumers rewritten off property syntax + `dynamic` (ComApplier, LookupHelper, WindowsFirewallRuleService, WindowsFirewallProfileService). **Verified live read-only** (harness on the production services): 272 inbound + 227 outbound real rules through activation → `get_Rules` → `IEnumVARIANT` → every `get_` accessor (VARIANT_BOOL, enums, the Interfaces VARIANT, indirect-string names), 74 predefined groups, policy `LocalPolicyModifyState`/`IsRuleGroupEnabled`, all 3 profiles + `ExcludedInterfaces`; the write path is adjacent vtable slots (`put_X`=`get_X`+1), correct by construction. **The M3 gate (0 `dynamic`, 0 `GetTypeFromProgID/CLSID`, 0 `[ComImport]`) is now met across ALL of Core — every COM interop is source-generated.** The only remaining WF item is **Half B = the M2-deferred MMI→`IWbemServices` CIM write path** (`Microsoft.Management.Infrastructure` — ~489 sites in 16 WF files: ConnectionSecurity, Profiles, Monitoring; instance CRUD `PutInstance`/`CreateInstance`/`ModifyInstance` with embedded instance arrays), which clears M2's second grep gate (`0 Microsoft.Management.Infrastructure`), not M3. It stays as the documented isolated interim until scoped as its own effort. |
| **M4 — package replacements + cutover** | LusrMgr → NetAPI32; PerfMon → PDH; drop `System.DirectoryServices*`, `System.Diagnostics.PerformanceCounter`; flip the default publish from R2R to `PublishAot` and retire the interim wording in docs | Run D equivalent publish is warning-clean in first-party code, all features smoke-tested under AOT; default publish = AOT |

Sequencing rationale: M0 is mechanical and unblocks M1 (the original startup crash was XAML-side, and clearing M0 proved sufficient to boot).
M2 before M3 because WMI replacements are self-contained per feature, while M3 (AzMan) is the
single largest rewrite. Features remain shippable throughout because the default publish stays
R2R until the M4 gate.

## Known upstream risks (track, do not block on)

- WinUI/XAML `{Binding}` and metadata rooting under AOT are still maturing upstream (WASDK 1.6
  release notes: "Later releases will enhance both C#/WinRT and the XAML Compiler to automate
  rooting").
- .NET runtime GC-race navigation hang under AOT:
  [dotnet/runtime#104582](https://github.com/dotnet/runtime/issues/104582) — re-test at each
  SDK/WASDK upgrade.
